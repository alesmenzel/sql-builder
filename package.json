{
  "name": "@alesmenzel/sql-builder",
  "version": "1.0.0",
  "description": "Write SQL as code.",
  "main": "dist/sql-builder.js",
  "types": "dist/declatations/sql-builder.d.ts",
  "scripts": {
    "test:unit": "jest",
    "docs:dev": "(cd documentation-website && npm start)",
    "docs:install": "(cd documentation-website && npm run build)",
    "docs:build": "(cd documentation-website && npm run build)",
    "build": "rm -rf dist && tsc",
    "test": "npm run test:unit && npm run docs:install && npm run docs:build"
  },
  "keywords": [
    "sql",
    "builder",
    "postgres"
  ],
  "author": "Aleš Menzel (alesmenzel@gmail.com)",
  "license": "MIT",
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  }
}
